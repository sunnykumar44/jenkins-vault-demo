pipeline {
    agent any

    stages {

        stage('Clone & Execute') {
            steps {

                sh '''
                chmod +x deploy.sh
                ./deploy.sh
                '''
            }
        }
    }
}